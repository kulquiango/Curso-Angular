/* tslint:disable:no-unused-variable */
import { ThemeDirective } from './theme.directive';

describe('Directive: Theme', () => {
  it('should create an instance', () => {
    const directive = new ThemeDirective();
    expect(directive).toBeTruthy();
  });
});
